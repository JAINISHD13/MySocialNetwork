<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/backimage"
    tools:context=".Activity.FindFriendsActivity">

    <include
        android:id="@+id/find_groups_appbar_layout"
        layout="@layout/app_bar_layout"
        ></include>
    <RelativeLayout
        android:id="@+id/myLayout"
        android:layout_below="@+id/find_groups_appbar_layout"
        android:layout_width="match_parent"
        android:layout_marginTop="15dp"
        android:layout_height="wrap_content">
        <!--
                <TextView
                    android:id="@+id/simpleTextId"
                    android:layout_marginLeft="20dp"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_alignParentRight="true"
                    android:text="Search for people from the University!"
                    android:textColor="@android:color/background_light"
                    android:textSize="18sp"
                    android:textStyle="bold"
                    />

                <android.support.v7.widget.SearchView
                    android:id="@+id/searchFriends"
                    android:layout_below="@+id/simpleTextId"
                    android:background="@drawable/inputs"
                    android:gravity="right"
                    android:layout_width="match_parent"
                    android:layout_height="50dp"
                    app:menu="@menu/menu"
                    >
                </android.support.v7.widget.SearchView>-->
    </RelativeLayout>

    <android.support.v7.widget.RecyclerView
        android:id="@+id/search_result_group_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="20dp"
        android:layout_alignParentStart="true"
        android:layout_alignParentLeft="true"
        android:layout_below="@id/myLayout"
        android:layout_margin="10dp"
        >
    </android.support.v7.widget.RecyclerView>
</RelativeLayout>